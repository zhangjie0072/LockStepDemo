using UnityEngine;
using System.Collections;


// this script is used for adjust the model scale size for GD.
public class ModelScale : MonoBehaviour {

    
    public float scaleSize = 0.5f;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
